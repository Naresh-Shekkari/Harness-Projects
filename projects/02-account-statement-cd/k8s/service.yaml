apiVersion: v1
kind: Service
metadata:
  name: account-statement-service
  namespace: default
  labels:
    app: account-statement-service
spec:
  type: NodePort
  selector:
    app: account-statement-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      name: http
